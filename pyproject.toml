[project]
name = "fastapi-ci-demo"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn",
    "httpx2",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
    "pre-commit",
]